# torch/torchaudio (CPU wheels) are installed separately in the Dockerfile
# before this file, so pip doesn't pull a CUDA build demucs would otherwise
# prefer.
#
# Talks to Supabase over plain HTTP (requests) rather than the supabase-py
# SDK — that SDK's client-side key format validation rejects this project's
# new-format sb_secret_... service role key. See main.py's module docstring.
demucs==4.0.1
yt-dlp>=2024.8.6
requests>=2.31

# torchaudio delegates file writing to soundfile on the pinned version below
# (see Dockerfile) — without it, saving the separated stems fails.
soundfile>=0.12.1
